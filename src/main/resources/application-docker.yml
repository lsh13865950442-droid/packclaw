server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: packclaw

  datasource:
    url: jdbc:postgresql://localhost:5432/packclaw?currentSchema=public
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:lsh123456}
    type: com.alibaba.druid.pool.DruidDataSource

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath*:mapper/*.xml

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.packclaw

packclaw:
  workspace:
    path: ${HOME}/.packclaw/workspace
  skill:
    repository:
      path: ${HOME}/.packclaw/skill-repository
  qiniu:
    access-key: En_O-Lm_Bhk3IVxGV44B_kDORGqWgkMTKfPLtweb
    secret-key: YWCeXhK1UsBmPiroQ7LqCN4cBYlzDrHScq7KbR2e
    bucket: packclaw
    # 千牛云对象存储的访问域名（需要根据实际情况修改）
    domain: http://tfn8jmas4.hd-bkt.clouddn.com